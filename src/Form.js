import React from 'react'

function Form(props) {
    const { values, submit, input, disabled, errors } = props;

    return (
        <form id = "pizza-form">
            <header>
                <h1>Build Your Own Pizza</h1>
            </header>
            <div>
                <label>

                </label>
            </div>
        </form>
    );
}

export default Form;