import React from 'react'

function Form(props) {
    const { formValues, submit } = props;

    return (
        <form id = "pizza-form">
            <div>
                <label>

                </label>
            </div>
        </form>
    );
}

export default Form;